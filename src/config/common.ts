export enum marriageStatus {
  SINGLE = 0,
  MARRIED = 1,
  DIVORCED = 2
}

export enum gender {
  MALE = 0,
  FEMALE = 1,
  NOT_APPLY = 2
}

export enum businessType {
  
}

export enum jobTitle {
  SALESPERSON = 1,
  STORE_MANAGER = 2,
  REGION_MANAGER = 3
}